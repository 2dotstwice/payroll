index
<div>
    {% if error is defined %}
        <p>{{  error }}</p>
    {% endif %}
    {% if app.user %}
        <p>Hello {{ app.user.username }}! Your email is {{ app.user.email }}</p>
        <p>Please login using your 2dotstwice account.</p>
        <a href="{{ logout_path }}">Logout</a>
    {% else %}
        <ul>
            <li><a href="{{ login_paths.google }}">Login with Google</a></li>
        </ul>
    {% endif %}
</div>